<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mapper.UserMapper">

    <!-- password는 의도적으로 제외한다. 해시가 조회 응답에 실리지 않도록
         인증용 selectByLoginId 에서만 별도로 가져온다. -->
    <sql id="userColumns">
        id, username, email, login_id, created_at, updated_at
    </sql>

    <select id="selectById" parameterType="long" resultType="UserDto">
        SELECT <include refid="userColumns"/>
        FROM users
        WHERE id = #{id}
    </select>

    <select id="selectPage" resultType="UserDto">
        SELECT <include refid="userColumns"/>
        FROM users
        ORDER BY id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM users
    </select>

    <select id="selectByLoginId" parameterType="string" resultType="UserDto">
        SELECT <include refid="userColumns"/>, password
        FROM users
        WHERE login_id = #{loginId}
    </select>

    <insert id="insert" parameterType="UserDto" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, email, login_id, password, created_at, updated_at)
        VALUES (#{username}, #{email}, #{loginId}, #{password}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="UserDto">
        UPDATE users
        SET username = #{username},
            email = #{email},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM users WHERE id = #{id}
    </delete>

</mapper>
