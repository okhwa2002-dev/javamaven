<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mapper.RefreshTokenMapper">

    <insert id="insert" parameterType="RefreshToken" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO refresh_tokens (user_id, token_hash, expires_at, created_at)
        VALUES (#{userId}, #{tokenHash}, #{expiresAt}, NOW())
    </insert>

    <select id="selectByHash" parameterType="string" resultType="RefreshToken">
        SELECT id, user_id, token_hash, expires_at, revoked_at, created_at
        FROM refresh_tokens
        WHERE token_hash = #{tokenHash}
    </select>

    <update id="revokeById" parameterType="long">
        UPDATE refresh_tokens
        SET revoked_at = NOW()
        WHERE id = #{id}
          AND revoked_at IS NULL
    </update>

    <delete id="deleteExpired">
        DELETE FROM refresh_tokens
        WHERE expires_at &lt; NOW()
    </delete>

</mapper>
